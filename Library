local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local CurrentCamera = workspace.CurrentCamera

-- Load WindUI dengan lebih aman
local WindUI = getgenv().WindUI

if not WindUI then
    local function LoadWindUI()
        local success, result = pcall(function()
            return loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
        end)
        
        if success and result then
            return result
        else
            warn("Failed to load WindUI: " .. tostring(result))
            return nil
        end
    end
    
    WindUI = LoadWindUI()
    if WindUI then
        getgenv().WindUI = WindUI
    else
        error("WindUI failed to load! Please check your internet connection.")
        return
    end
end

-- Cek apakah hub sudah terbuka
if getgenv().DAMEUNGRR_HUB then
    getgenv().DAMEUNGRR_HUB:Show()
    return
end

-- Fungsi sound dengan error handling
local function PlaySound(soundId, volume, speed)
    pcall(function()
        local sound = Instance.new("Sound")
        sound.SoundId = "rbxassetid://" .. soundId
        sound.Volume = volume or 1
        sound.PlaybackSpeed = speed or 1
        sound.Parent = workspace
        sound:Play()
        
        task.delay(5, function()
            pcall(function() sound:Destroy() end)
        end)
    end)
end

-- Get safe parent dengan lebih aman
local function GetSafeParent()
    local success, result = pcall(function()
        if gethui then
            return gethui()
        elseif syn and syn.protect_gui then
            local gui = Instance.new("ScreenGui")
            syn.protect_gui(gui)
            return game:GetService("CoreGui")
        else
            return game:GetService("CoreGui")
        end
    end)
    
    if success and result then
        return result
    end
    return game:GetService("CoreGui")
end

-- Intro dengan error handling
local function InitIntro()
    pcall(function()
        local screenGui = Instance.new("ScreenGui")
        screenGui.Name = "DameungrrIntro_Cinematic"
        screenGui.IgnoreGuiInset = true
        screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
        screenGui.Parent = GetSafeParent()

        local blurEffect = Instance.new("BlurEffect")
        blurEffect.Size = 0
        blurEffect.Parent = Lighting

        local mainFrame = Instance.new("Frame")
        mainFrame.Parent = screenGui
        mainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        mainFrame.BackgroundTransparency = 1
        mainFrame.Size = UDim2.new(1, 0, 1, 0)
        mainFrame.ZIndex = 1

        local titleContainer = Instance.new("Frame")
        titleContainer.Parent = mainFrame
        titleContainer.BackgroundTransparency = 1
        titleContainer.AnchorPoint = Vector2.new(0.5, 0.5)
        titleContainer.Position = UDim2.new(0.5, 0, 0.45, 0)
        titleContainer.Size = UDim2.new(0, 400, 0, 150)
        titleContainer.ZIndex = 2

        local titleLayout = Instance.new("UIListLayout")
        titleLayout.Parent = titleContainer
        titleLayout.FillDirection = Enum.FillDirection.Horizontal
        titleLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        titleLayout.VerticalAlignment = Enum.VerticalAlignment.Center
        titleLayout.Padding = UDim.new(0, 15)
        titleLayout.SortOrder = Enum.SortOrder.LayoutOrder

        local function CreateTitleLetter(letter, color)
            local letterFrame = Instance.new("Frame")
            letterFrame.Parent = titleContainer
            letterFrame.BackgroundTransparency = 1
            letterFrame.Size = UDim2.new(0, 80, 0, 100)

            local mainText = Instance.new("TextLabel")
            mainText.Parent = letterFrame
            mainText.AnchorPoint = Vector2.new(0.5, 0.5)
            mainText.Position = UDim2.new(0.5, 0, 0.5, 0)
            mainText.Size = UDim2.new(0, 80, 0, 100)
            mainText.BackgroundTransparency = 1
            mainText.Font = Enum.Font.GothamBlack
            mainText.Text = letter
            mainText.TextColor3 = color
            mainText.TextSize = 100
            mainText.TextTransparency = 1
            mainText.ZIndex = 3

            local shadowText = Instance.new("TextLabel")
            shadowText.Parent = letterFrame
            shadowText.AnchorPoint = Vector2.new(0.5, 0.5)
            shadowText.Position = UDim2.new(0.5, 0, 0.5, 0)
            shadowText.Size = UDim2.new(1, 0, 1, 0)
            shadowText.BackgroundTransparency = 1
            shadowText.Font = Enum.Font.GothamBlack
            shadowText.Text = letter
            shadowText.TextColor3 = Color3.fromRGB(139, 0, 0)
            shadowText.TextSize = 105
            shadowText.TextTransparency = 1
            shadowText.ZIndex = 2

            return mainText, shadowText
        end

        local letterD, shadowD = CreateTitleLetter("D", Color3.fromRGB(255, 0, 0))
        local letterU, shadowU = CreateTitleLetter("U", Color3.fromRGB(200, 200, 200))
        local letterH, shadowH = CreateTitleLetter("H", Color3.fromRGB(255, 0, 0))

        local subtitleFrame = Instance.new("Frame")
        subtitleFrame.Parent = mainFrame
        subtitleFrame.BackgroundTransparency = 1
        subtitleFrame.AnchorPoint = Vector2.new(0.5, 0)
        subtitleFrame.Position = UDim2.new(0.5, 0, 0.55, 30)
        subtitleFrame.Size = UDim2.new(0, 500, 0, 40)
        subtitleFrame.ClipsDescendants = false
        subtitleFrame.ZIndex = 2

        local subtitleText = Instance.new("TextLabel")
        subtitleText.Parent = subtitleFrame
        subtitleText.BackgroundTransparency = 1
        subtitleText.Size = UDim2.new(1, 0, 1, 0)
        subtitleText.Position = UDim2.new(0, 0, 1, 0)
        subtitleText.Font = Enum.Font.GothamBold
        subtitleText.Text = "D A M E U N G R R H U B"
        subtitleText.TextColor3 = Color3.fromRGB(255, 0, 0)
        subtitleText.TextSize = 18
        subtitleText.TextTransparency = 1
        subtitleText.ZIndex = 3
        subtitleText.TextStrokeTransparency = 0.3
        subtitleText.TextStrokeColor3 = Color3.fromRGB(200, 0, 0)

        local flashFrame = Instance.new("Frame")
        flashFrame.Parent = screenGui
        flashFrame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        flashFrame.BackgroundTransparency = 1
        flashFrame.Size = UDim2.new(1, 0, 1, 0)
        flashFrame.ZIndex = 10

        -- Play sounds with error handling
        pcall(function() PlaySound("12222216", 2, 0.2) end)

        TweenService:Create(mainFrame, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
            BackgroundTransparency = 0
        }):Play()
        
        TweenService:Create(blurEffect, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
            Size = 25
        }):Play()
        
        task.wait(0.8)

        local function AnimateLetter(mainText, shadowText)
            TweenService:Create(mainText, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Size = UDim2.new(1, 0, 1, 0),
                TextTransparency = 0
            }):Play()
            
            TweenService:Create(shadowText, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                TextTransparency = 0.4
            }):Play()
        end

        pcall(function() PlaySound("12222076", 2, 0.8) end)
        AnimateLetter(letterD, shadowD)
        task.wait(0.35)
        
        pcall(function() PlaySound("12222076", 2, 1) end)
        AnimateLetter(letterU, shadowU)
        task.wait(0.35)
        
        pcall(function() PlaySound("12222076", 2, 1.2) end)
        AnimateLetter(letterH, shadowH)
        task.wait(0.4)

        pcall(function() PlaySound("12222200", 3, 0.5) end)
        flashFrame.BackgroundTransparency = 0.3
        TweenService:Create(flashFrame, TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
            BackgroundTransparency = 1
        }):Play()
        
        pcall(function() PlaySound("12222222", 1.5, 1.2) end)
        
        TweenService:Create(subtitleText, TweenInfo.new(0.8, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
            Position = UDim2.new(0, 0, 0, 0),
            TextTransparency = 0
        }):Play()

        TweenService:Create(titleContainer, TweenInfo.new(0.1, Enum.EasingStyle.Bounce, Enum.EasingDirection.Out, 2, true), {
            Position = UDim2.new(0.5, 0, 0.44, 0)
        }):Play()
        
        task.wait(2.5)
        pcall(function() PlaySound("12222216", 2.5, 0.35) end)
        task.wait(0.5)

        local outroTween = TweenInfo.new(0.7, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
        
        TweenService:Create(letterD, outroTween, {
            Position = UDim2.new(0.5, -150, 0.5, -100),
            Rotation = -25,
            TextTransparency = 1,
            TextSize = 140
        }):Play()
        
        TweenService:Create(shadowD, outroTween, {
            Position = UDim2.new(0.5, -150, 0.5, -100),
            Rotation = -25,
            TextTransparency = 1,
            TextSize = 145
        }):Play()
        
        TweenService:Create(letterU, outroTween, {
            Position = UDim2.new(0.5, 0, 0.5, -150),
            Rotation = 15,
            TextTransparency = 1,
            TextSize = 140
        }):Play()
        
        TweenService:Create(shadowU, outroTween, {
            Position = UDim2.new(0.5, 0, 0.5, -150),
            Rotation = 15,
            TextTransparency = 1,
            TextSize = 145
        }):Play()
        
        TweenService:Create(letterH, outroTween, {
            Position = UDim2.new(0.5, 150, 0.5, -100),
            Rotation = 30,
            TextTransparency = 1,
            TextSize = 140
        }):Play()
        
        TweenService:Create(shadowH, outroTween, {
            Position = UDim2.new(0.5, 150, 0.5, -100),
            Rotation = 30,
            TextTransparency = 1,
            TextSize = 145
        }):Play()
        
        TweenService:Create(subtitleFrame, outroTween, {
            Position = UDim2.new(0.5, 0, 0.55, 120),
            Rotation = -10
        }):Play()
        
        TweenService:Create(subtitleText, outroTween, {
            TextTransparency = 1
        }):Play()

        TweenService:Create(mainFrame, TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
            BackgroundTransparency = 1
        }):Play()
        
        TweenService:Create(blurEffect, TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
            Size = 0
        }):Play()
        
        task.wait(1)
        pcall(function() blurEffect:Destroy() end)
        pcall(function() screenGui:Destroy() end)
    end)
end

-- Run intro dengan error handling
pcall(function() InitIntro() end)

task.wait(6)

-- Create Window dengan error handling
local Window
pcall(function()
    Window = WindUI:CreateWindow({
        Title = "DAMEUNGRR HUB",
        Folder = "dameungrr-hub",
        Icon = "solar:list-bold-duotone",
        NewElements = true,
        Background = "rbxassetid://119581794827602",
        HideSearchBar = false,
        OpenButton = {
            Title = "Dameungrr hub",
            CornerRadius = UDim.new(1, 0),
            StrokeThickness = 3,
            Enabled = true,
            Draggable = true,
            OnlyMobile = false,
            Scale = 0.5,
            Color = ColorSequence.new(
                Color3.fromHex("#FF3030"),
                Color3.fromHex("#FF2F2F")
            ),
        },
        Topbar = {
            Height = 44,
            ButtonsType = "Mac",
        },
    })
end)

if not Window then
    error("Failed to create Window! Please check WindUI installation.")
    return
end

getgenv().DAMEUNGRR_HUB = Window

-- Create tabs with error handling
local HomeTab, ScriptsTab, SettingsTab, AboutTab

pcall(function()
    HomeTab = Window:Tab({
        Title = "Home",
        Icon = "solar:home-2-bold",
        IconColor = Color3.fromHex("#FF3030"),
        IconShape = "Square",
        Border = true,
    })
end)

pcall(function()
    ScriptsTab = Window:Tab({
        Title = "Scripts",
        Icon = "solar:code-bold",
        IconColor = Color3.fromHex("#00D4FF"),
        IconShape = "Square",
        Border = true,
    })
end)

pcall(function()
    SettingsTab = Window:Tab({
        Title = "Settings",
        Icon = "solar:settings-bold",
        IconColor = Color3.fromHex("#ECA201"),
        IconShape = "Square",
        Border = true,
    })
end)

pcall(function()
    AboutTab = Window:Tab({
        Title = "About",
        Icon = "solar:info-square-bold",
        IconColor = Color3.fromHex("#FF3030"),
        IconShape = "Square",
        Border = true,
    })
end)

-- Home Tab Content
if HomeTab then
    pcall(function()
        local HomeSection = HomeTab:Section({
            Title = "🏠 WELCOME TO DAMEUNGRR HUB",
            TextSize = 18,
            FontWeight = Enum.FontWeight.Medium,
        })

        HomeSection:Image({
            Image = "https://repository-images.githubusercontent.com/880118829/22c020eb-d1b1-4b34-ac4d-e33fd88db38d",
            AspectRatio = "16:9",
            Radius = 12,
        })

        HomeSection:Space()

        HomeSection:Section({
            Title = "✨ WindUI Premium by Umemaro",
            TextSize = 16,
            TextTransparency = 0.3,
            FontWeight = Enum.FontWeight.Medium,
        })

        HomeSection:Space({ Columns = 2 })

        HomeSection:Button({
            Title = "🚀 Welcome",
            Color = Color3.fromHex("#10C550"),
            Justify = "Center",
            Icon = "rocket",
            Callback = function()
                WindUI:Notify({
                    Title = "Welcome!",
                    Content = "Welcome para member!",
                    Icon = "solar:bell-bold",
                    Duration = 3,
                })
            end,
        })

        HomeSection:Button({
            Title = "👥 View Credits",
            Color = Color3.fromHex("#7775F2"),
            Justify = "Center",
            Icon = "users",
            Callback = function()
                WindUI:Notify({
                    Title = "Credits",
                    Content = "Developed by Umemaro Owner Dameungrr hub",
                    Icon = "solar:info-circle-bold",
                    Duration = 3,
                })
            end,
        })
    end)
end

-- Scripts Tab Content
if ScriptsTab then
    pcall(function()
        local ScriptsSection = ScriptsTab:Section({
            Title = "📜 My Script",
            TextSize = 18,
            FontWeight = Enum.FontWeight.Medium,
        })

        local ScriptGrid = ScriptsTab:Grid({
            Columns = 2,
            Spacing = 15,
        })

        ScriptGrid:Paragraph({
            Title = "🚀 Speed Hub",
            Desc = "Advanced movement script with speed boost",
            Image = "solar:rocket-bold",
            Buttons = {
                {
                    Title = "Execute",
                    Color = Color3.fromHex("#10C550"),
                    Callback = function()
                        WindUI:Notify({
                            Title = "Executing Speed Hub",
                            Content = "Speed Hub script loaded successfully!",
                            Duration = 2,
                        })
                    end,
                },
                {
                    Title = "Info",
                    Color = Color3.fromHex("#257AF7"),
                    Callback = function()
                        WindUI:Popup({
                            Title = "Speed Hub Info",
                            Content = "Advanced movement script with customizable speed settings.",
                            Buttons = {
                                { Title = "Close", Variant = "Tertiary" },
                            },
                        })
                    end,
                },
            },
        })

        ScriptGrid:Paragraph({
            Title = "⚔️ Combat Pro",
            Desc = "Advanced combat system with auto-aim",
            Image = "solar:sword-bold",
            Buttons = {
                {
                    Title = "Execute",
                    Color = Color3.fromHex("#10C550"),
                    Callback = function()
                        WindUI:Notify({
                            Title = "Executing Combat Pro",
                            Content = "Combat Pro script loaded successfully!",
                            Duration = 2,
                        })
                    end,
                },
                {
                    Title = "Info",
                    Color = Color3.fromHex("#257AF7"),
                    Callback = function()
                        WindUI:Popup({
                            Title = "Combat Pro Info",
                            Content = "Complete combat system with auto-aim, damage boost, and more.",
                            Buttons = {
                                { Title = "Close", Variant = "Tertiary" },
                            },
                        })
                    end,
                },
            },
        })
    end)
end

-- Settings Tab Content
if SettingsTab then
    pcall(function()
        local GeneralSettings = SettingsTab:Section({
            Title = "⚙️ General Settings",
            TextSize = 16,
            FontWeight = Enum.FontWeight.Medium,
        })

        GeneralSettings:Toggle({
            Title = "Auto-Execute Scripts",
            Desc = "Automatically execute scripts on join",
            Value = true,
            Callback = function(state)
                print("Auto-Execute: " .. tostring(state))
            end,
        })

        GeneralSettings:Space()

        GeneralSettings:Toggle({
            Title = "Show Notifications",
            Desc = "Display notifications when scripts execute",
            Value = true,
            Callback = function(state)
                print("Show Notifications: " .. tostring(state))
            end,
        })
    end)
end

-- About Tab Content
if AboutTab then
    pcall(function()
        local AboutSection = AboutTab:Section({
            Title = "ℹ️ About DAMEUNGRR HUB",
            TextSize = 18,
            FontWeight = Enum.FontWeight.Medium,
        })

        AboutSection:Image({
            Image = "https://repository-images.githubusercontent.com/880118829/22c020eb-d1b1-4b34-ac4d-e33fd88db38d",
            AspectRatio = "16:9",
            Radius = 12,
        })

        AboutSection:Space({ Columns = 3 })

        AboutSection:Section({
            Title = "DAMEUNGRR HUB",
            TextSize = 18,
            FontWeight = Enum.FontWeight.Medium,
        })

        AboutSection:Space()

        AboutSection:Section({
            Title = "🎮 The Ultimate Roblox Script Hub\n\nDAMEUNGRR HUB is a premium script hub designed for Roblox players who want to enhance their gaming experience.",
            TextSize = 14,
            TextTransparency = 0.3,
            FontWeight = Enum.FontWeight.Medium,
        })

        AboutSection:Space({ Columns = 2 })

        AboutSection:Button({
            Title = "💬 Join Discord",
            Color = Color3.fromHex("#7775F2"),
            Justify = "Center",
            Icon = "discord",
            Callback = function()
                if setclipboard then
                    setclipboard("https://discord.gg/RZKEdAvkF")
                    WindUI:Notify({
                        Title = "Discord Link Copied!",
                        Content = "Join our Discord community!",
                        Duration = 3,
                    })
                end
            end,
        })

        AboutSection:Button({
            Title = "❤️ Support Us",
            Color = Color3.fromHex("#EF4F1D"),
            Justify = "Center",
            Icon = "heart",
            Callback = function()
                WindUI:Notify({
                    Title = "Thank You!",
                    Content = "Your support means a lot to us! ❤️",
                    Duration = 4,
                })
            end,
        })
    end)
end

-- Final notification
task.wait(0.5)
pcall(function()
    WindUI:Notify({
        Title = "Dameungrr hub Loaded!",
        Content = "Welcome to Dameungrr hub",
        Duration = 4,
        Icon = "solar:check-circle-bold",
    })
end)

print("✅ DAMEUNGRR HUB successfully loaded!")
